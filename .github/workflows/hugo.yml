# .github/workflows/main.yml
name: Publish Site

on:
  push:
    branches: 
      - main
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
        with:
          submodules: true
          fetch-depth: 0
      - name: Publish Site
        uses: chabad360/hugo-gh-pages@master
        with:
          githubToken: ${{ secrets.ACTIONS_TOKEN }}
          branch: main
          hugoVersion: 'extended_0.97.0'
          cname: https://go.face2ai.com
          repo: Tony-Tan/face2ai_go
